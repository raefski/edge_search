# One pick'em line snapshot -- the instance name IS the snapshot label.
#
#   cp deploy/pickem-capture@.service deploy/pickem-capture-*.timer \
#      ~/.config/systemd/user/
#   systemctl --user daemon-reload
#   systemctl --user enable --now pickem-capture-post.timer \
#     pickem-capture-lock-wed.timer pickem-capture-lock-thu.timer \
#     pickem-capture-midweek.timer pickem-capture-lock-sun.timer \
#     pickem-capture-lock-mon.timer
#   systemctl --user list-timers 'pickem-capture*'
#
# Same linger requirement as arb-agent.service:
#   sudo loginctl enable-linger $USER
#
# WHY THIS RUNS HERE AND NOT IN GITHUB ACTIONS
# .github/workflows/pickem-capture.yml was built for this job, but it cannot do
# it for free: the scraped board lives in data/odds.db, which is gitignored and
# 1.5GB, and a runner cannot rebuild it because DraftKings 403s datacenter IPs
# (the same block ODDS_LAYER.md and PICKEM_STATUS.md both document). A runner
# can therefore only capture through the PAID Odds API. This desktop already
# scrapes the board on odds-collect-pickem-nfl.timer, so it is the only machine
# that can bank a reading for nothing. The workflow stays as a paid fallback.
#
# COMMITTING THE DATA, AND AUTO-PUSH (decided 2026-09-16)
# git IS this file's database: PICKEM_WEEKLY.md's own table says an
# uncommitted row does not survive, and the Streamlit page reads the repo.
# --push WAS deliberately left off, on the reasoning that a timer pushing to
# this public repo unattended was a decision the owner had to make once,
# explicitly. That decision surfaced its own cost before it got made: three
# real deadline captures (2026-09-13 lock-sun, -14 lock-mon, -15's Week 2
# post) wrote good local data that sat uncommitted for days, and the deployed
# Streamlit app silently served Week 1's lock-thu snapshot the whole time --
# nothing said so, because nothing was watching for "captured but not
# pushed." Adam confirmed auto-push explicitly on 2026-09-16; both halves now
# push (ExecStartPost below, and pickem_pool_fetch.py's own --push). The
# content is the same category PICKEM_STATUS.md already calls safe to commit
# -- public spread/total/community-pct data, no picks, no standings, no
# money -- so the "public repo" concern was about the ACT of push being
# unattended, not about what it publishes.
#
# WHY ExecStartPre COLLECTS FIRST -- the finding that made these timers wrong
# The board is collected by odds-collect-pickem-nfl.timer at 12:20 and 23:20
# ONLY. Every capture therefore read a board hours old and, until 2026-09-09,
# stamped it `captured_at = utcnow()`:
#
#   lock-sun   fired Sun 12:00 -> Saturday 23:20 prices   (12h40m stale)
#   midweek    fired Fri 12:00 -> Thursday  23:20 prices  (12h40m stale)
#   the 19:00 locks             -> that day's 12:20 board  (6h40m stale)
#   post       fired Tue 13:15 -> the 12:20 board, i.e. BEFORE CBS's 1pm freeze
#
# edge/odds/profiles.py gives pickem_nfl max_age_seconds=86400 on purpose (the
# pool line is frozen all week), so the freshness contract never objected. The
# model's input is `live_line - pool_line`, so WHEN the board was read is a
# model parameter, and PICKEM_MODEL.md section 6 ranks "capture as late as
# legally possible" as the single largest remaining upside.
#
# The leading `-` is load-bearing: if the collection fails (DraftKings 403,
# network down) the capture must STILL run and bank the older board -- now
# honestly stamped, and with board_age_seconds recording the gap. A stale
# reading recorded truthfully beats no reading, which is gone forever.
#
# THE SAME PRINCIPLE NOW APPLIES TO THE CBS HALF (2026-09-10). See
# scripts/pickem_pool_fetch.py's ExecStartPre in [Service] below: it is
# soft-failing for the identical reason, and for a harder version of it --
# CBS sessions expire outright, not just go briefly stale, and nothing can
# renew one without a human. The market half must be as indifferent to that
# failing as it already is to the board collector failing.
[Unit]
Description=Capture pick'em line snapshot %i
After=network-online.target
# A failed capture used to be completely silent: Restart=no, `raise
# SystemExit(1)` on any fetch error, no OnFailure anywhere in deploy/ or
# ~/.config/systemd/user/, and no capture timer had ever executed. A StaleOdds
# at Sunday noon loses the week's most valuable reading and nothing says so.
OnFailure=pickem-capture-failed@%i.service
StartLimitIntervalSec=3600
StartLimitBurst=3

[Service]
Type=oneshot
WorkingDirectory=%h/edge_search
# CBS HALF, AUTOMATED (2026-09-10). Fetches this week's pool page with a
# stored login session (scripts/pickem_session_bootstrap.py) and merges it
# into pickem_current_week.csv -- see scripts/pickem_pool_fetch.py. Leading
# `-`: a dead/expired session (they do not last forever, and nothing can
# renew one unattended) must NEVER cancel the capture below, which is the
# time-critical half and the one that can never be retaken.
ExecStartPre=-/usr/bin/env python3 scripts/pickem_pool_fetch.py --week auto --write --push
ExecStartPre=-/usr/bin/env python3 scripts/odds_collect.py --profile pickem_nfl
# --market-only: bank the market reading UNCONDITIONALLY, whether or not the
# CBS fetch above actually got anything. The two halves join on (season,
# week, snapshot, home_team), so ExecStartPost below still merges in
# whatever CBS half exists the moment this line runs -- but this line itself
# must never be made to wait on it.
ExecStart=/usr/bin/env python3 scripts/pickem_capture.py \
    --snapshot %i --week auto --market-only --confirm
# Immediately re-run the SAME label without --market-only, so a CBS row
# fetched above (or already on file from Tuesday) gets merged into the row
# ExecStart just banked -- automating what used to be "run this again by
# hand once you've pasted the pool page" (PICKEM_WEEKLY.md). Strictly
# additive: pickem_log.complete only fills columns never measured and never
# overwrites one that was, so this can only add data, never corrupt the
# market-only row above. Leading `-`: must never fail the unit either --
# an empty CBS half here just means "wrote 0 new, completed N", not an error.
# --push lives HERE, not on ExecStart above: this is the pass that produces
# the row's final state for this label (market + whatever CBS half exists),
# so one push per capture rather than an incomplete one immediately
# superseded by a complete one seconds later.
#
# The leading `-` means a PUSH failure here (network, a rebase conflict) does
# NOT flip the unit to failed and does not trigger OnFailure/Restart -- only
# a market-fetch failure does that, via ExecStart's hard exit above. That is
# a deliberate, different severity: the market reading, if missed, is gone
# forever; a failed push is not -- push_snapshot commits the file's WHOLE
# current diff, so the very next capture's --push (hours away at most)
# catches up anything this one couldn't send. pickem_capture.py's own
# push-failure branch still exits 1 and prints why, so `journalctl --user -u
# pickem-capture@%i.service` shows it even though the unit reports success.
ExecStartPost=-/usr/bin/env python3 scripts/pickem_capture.py \
    --snapshot %i --week auto --confirm --push
# A deadline reading cannot be taken again later, so a transient failure IS
# worth retrying -- but only briefly and only a few times, bounded by the
# StartLimit above. Five minutes apart, three attempts, then OnFailure records
# it where a human will see it (data/pickem_capture_failures.log, surfaced at
# the top of pages/4_🎯_Pickem.py).
Restart=on-failure
RestartSec=300
Nice=10
TimeoutStartSec=300
