# Copy to .env and fill in. .env is gitignored — never commit your key.
ODDS_API_KEY=your_the_odds_api_v4_key_here

# Optional: abort any paid call if remaining account credits fall below this.
EDGE_CREDITS_FLOOR=5000
